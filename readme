docker pull kjconroy/sqlc

docker run --rm -v ${pwd}:/src -w /src kjconroy/sqlc version

docker run --rm -v ${pwd}:/src -w /src kjconroy/sqlc init

docker run --rm -v ${pwd}:/src -w /src kjconroy/sqlc generate

go mod init simple_bank

go mod tidy

go test -coverprofile=coverage.out ./...

https://wiki.postgresql.org/wiki/Lock_Monitoring


-----------------------------------------------------------------------
git init

git add .

git commit -m "CI workflow"

git branch -M master

git remote add origin https://github.com/ivandrosilva12/small_bank.git

git push origin master
_______________________________________________________________________

